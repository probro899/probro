// import schema from '@probro/common/src/schema';
// import db from '../db';

// export default async function addBoard(record) {
//   const { session } = this;

//   try {
//     const res = await db.execute(async ({ insert, findOne }) => {
//       const userId = await insert('Board', record);
//       if (userId) {

//       }
//     })
//   } retu

// }