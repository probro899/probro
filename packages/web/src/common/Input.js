import React from 'react';
import { InputGroup } from '@blueprintjs/core';

export default ({placeholder,class_}) => {
    return (
            <InputGroup placeholder={placeholder} className={class_} />)
}